name: ${project.name}
version: ${project.version}
description: ${project.description}
author: ${project.organization.name}
website: ${project.url}
main: ${project.groupId}.${project.artifactId}.${project.name}
api-version: 1.15
softdepend: [ WorldEdit, FastAsyncWorldEdit, PlugMan ]
commands:
  schemmanager:
    description: The basic command for SchemManager
    aliases: [ sm ]